- service: web
  name: build_web_app
  command: node_modules/.bin/ng build --prod --aot

- service: awsdeployment
  tag: master
  name: staging_deploy_web
  command: aws s3 sync /app/dist s3://codeship-demo-site
